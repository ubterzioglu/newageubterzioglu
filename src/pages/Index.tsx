// Update this page (the content is just a fallback if you fail to update the page)

import { CinematicHeroNewAge } from "@/components/sections/CinematicHeroNewAge";

const Index = () => {
  return (
    <main>
      <CinematicHeroNewAge />
    </main>
  );
};

export default Index;
